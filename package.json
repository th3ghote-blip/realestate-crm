{
  "name": "realestate-crm",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm --prefix server run prisma:generate && npm --prefix client run build",
    "start": "cd server && npx --no-install prisma db push --accept-data-loss --skip-generate && node src/index.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  }
}
